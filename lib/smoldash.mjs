/**
 * @file smoldash.mjs
 * @module smoldash
 * @description A small lodash-like utility library.
 */
const _NEED = ['smoldash']; // dependencies
import { 
    at, 
    clone, 
    cloneDeep, 
    compact,
    every, 
    filter, 
    find, 
    findIndex,
    forEach, 
    flatten, 
    flow, 
    get, 
    groupBy, 
    has, 
    head,
    indexOf,
    isEmpty,
    isEqual,
    kebabCase,
    keyBy,
    map,
    merge,
    once,
    pickBy,
    range,
    some,
    take,
    sortBy, 
    uniq,
    uniqBy, 
    uniqueId
} from 'smoldash';

export {
    _NEED,
    at, 
    clone, 
    cloneDeep, 
    compact,
    every, 
    filter, 
    find, 
    findIndex,
    forEach, 
    flatten, 
    flow, 
    get, 
    groupBy, 
    has, 
    head,
    indexOf,
    isEmpty,
    isEqual,
    kebabCase,
    keyBy,
    map,
    merge,
    once,
    pickBy,
    range,
    some,
    take,
    sortBy, 
    uniq,
    uniqBy, 
    uniqueId
};